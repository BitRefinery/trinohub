"""End-to-end scenario modules for TrinoHub.

Each module is a self-contained workflow exercised through the real HTTP API
via :class:`testing.harness.E2EHarness`. Modules are numbered so the runner and
``unittest`` execute them in a sensible release-validation order.
"""
